# Kopyala: ava_group klasöründe ".env" olarak kaydet (Next.js ve Prisma burayı okur)
# SQLite — dosya prisma klasöründe oluşur
DATABASE_URL="file:./dev.db"

# Admin girişi (/admin). Boş bırakılırsa api/auth/login varsayılanları kullanılır.
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=admin123
